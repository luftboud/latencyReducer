# List of dependencies you'll need for sender

```shell
    brew install gstreamer
    brew install libnice-gstreamer
    brew install pygobject3
    brew install gobject-introspection
    brew install glib
```